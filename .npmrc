# npm configuration for consistent dependency management
# This ensures package-lock.json remains consistent across different environments

# Ensure lockfile version 3 (npm 7+)
lockfile-version=3

# Don't show funding messages
fund=false

# Don't run audit automatically (speeds up install)
audit=false

# Use legacy peer deps resolution to avoid conflicts
legacy-peer-deps=false
