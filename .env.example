# Database
# For SQLite (default): file:./dev.db (stored in prisma/ directory)
# For PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
# URL of your application
# For local development: http://localhost:3000
# For production with HTTPS: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# Secret for JWT encryption (generate with: openssl rand -base64 32)
# This will be auto-generated by the setup script
NEXTAUTH_SECRET="your-secret-here"

# HTTPS Configuration (optional)
# Set to "true" to enable HTTPS with Let's Encrypt
USE_HTTPS="false"

# Your domain name (required if USE_HTTPS is true)
DOMAIN=""
