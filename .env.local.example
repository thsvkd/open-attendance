# Server Port
# Port on which the Next.js development server will run (default: 3000)
PORT=3000

# Database
# For SQLite (default): file:./dev.db (stored in prisma/ directory)
# For PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
# URL of your application (automatically set based on PORT, or specify manually)
# Leave empty to auto-generate based on PORT variable above
NEXTAUTH_URL="http://localhost:3000"

# Secret for JWT encryption (generate with: openssl rand -base64 32)
# This will be auto-generated by the setup script
NEXTAUTH_SECRET="your-secret-here"

# Port for E2E tests
E2E_PORT=3001